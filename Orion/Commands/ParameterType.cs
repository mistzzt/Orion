namespace Orion.Commands
{
	/// <summary>
	/// Enumeration that describes the type of parameter in a command (not the value type of a parameter)
	/// </summary>
	public enum ParameterType
	{
		Positional = 0,
		Named = 1
	}
}
